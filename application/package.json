{
  "name": "foodlens-ai",
  "version": "1.0.0",
  "private": true,
  "description": "FoodLens AI - Microservices Food Analysis Platform",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build --workspace=packages/shared-types && npm run build --workspace=packages/shared-config && npm run build --workspace=packages/shared-utils",
    "build:services": "npm run build --workspace=services/api-gateway && npm run build --workspace=services/auth-service && npm run build --workspace=services/image-service && npm run build --workspace=services/food-service && npm run build --workspace=services/nutrition-service && npm run build --workspace=services/analysis-service && npm run build --workspace=services/recommendation-service",
    "build:apps": "npm run build --workspace=apps/frontend",
    "build": "npm run build:packages && npm run build:services && npm run build:apps",
    "test": "jest --passWithNoTests",
    "test:unit": "jest --config jest.config.json",
    "test:e2e": "node scripts/e2e-test.js",
    "lint": "eslint . --ext .ts,.tsx",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "prisma:generate": "prisma generate --schema=./prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema=./prisma/schema.prisma",
    "prisma:seed": "ts-node ./prisma/seeds/seed.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.24",
    "@typescript-eslint/eslint-plugin": "^7.1.1",
    "@typescript-eslint/parser": "^7.1.1",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "prisma": "^5.10.2",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@types/uuid": "^10.0.0",
    "uuid": "^14.0.2"
  }
}
