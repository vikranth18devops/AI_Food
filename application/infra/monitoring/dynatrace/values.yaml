# Dynatrace OneAgent Helm Values Override

secret:
  apiToken: "dt0c01.SAMPLE_DYNATRACE_API_TOKEN"

dynakube:
  apiUrl: "https://your-tenant.live.dynatrace.com/api"
  oneAgent:
    cloudNativeFullStack: {}
  activeGate:
    capabilities:
      - routing
      - kubernetes-monitoring
