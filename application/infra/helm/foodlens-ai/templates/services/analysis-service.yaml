apiVersion: v1
kind: Service
metadata:
  name: {{ include "foodlens-ai.fullname" . }}-analysis-service
  labels:
    app: analysis-service
    {{- include "foodlens-ai.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.services.analysisService.port }}
      targetPort: {{ .Values.services.analysisService.port }}
      protocol: TCP
      name: http
  selector:
    app: analysis-service
