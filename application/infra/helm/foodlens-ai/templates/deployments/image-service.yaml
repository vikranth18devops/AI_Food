apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "foodlens-ai.fullname" . }}-image-service
  labels:
    app: image-service
    {{- include "foodlens-ai.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.services.imageService.replicaCount }}
  selector:
    matchLabels:
      app: image-service
  template:
    metadata:
      labels:
        app: image-service
    spec:
      containers:
        - name: image-service
          image: "{{ .Values.image.registry }}/{{ .Values.image.repositoryPrefix }}/image-service:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.imageService.port }}
          envFrom:
            - configMapRef:
                name: {{ include "foodlens-ai.fullname" . }}-config
            - secretRef:
                name: {{ include "foodlens-ai.fullname" . }}-secrets
          resources:
            {{- toYaml .Values.services.imageService.resources | nindent 12 }}
