# 07 - Microservices Communication & VPC Access

Configure Serverless VPC Access Connector for internal Cloud Run communications:

```bash
gcloud compute networks vpc-access connectors list --region=us-central1
```
