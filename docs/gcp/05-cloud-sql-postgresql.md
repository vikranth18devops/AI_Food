# 05 - GCP Cloud SQL PostgreSQL Setup

Provision and manage Cloud SQL PostgreSQL 15 database instance:

```bash
gcloud sql instances list
gcloud sql databases list --instance=foodlens-dev-cloudsql
```
